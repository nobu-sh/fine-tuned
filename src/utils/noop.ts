/**
 * No Operation Function. Useful for empty catches to satify eslint.
 */
export const Noop = () => undefined;

/**
 * No Operation Function. Useful for empty catches to satify eslint.
 */
export const NoopFunction = Noop;
/**
 * No Operation Function. Useful for empty catches to satify eslint.
 */
export const Noop_Function = Noop;
